import React, {FC} from 'react'
import { View, Text, StyleSheet } from 'react-native';

export const SupportList: FC = () => {
    return (
        <View>
            <Text>Due to API's free tier, not all tickers are available for live-streaming data. Here is a supported list of tickers:</Text>
        </View>
    )
}


const styles = StyleSheet.create({

})